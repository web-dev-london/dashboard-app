export default interface InitialValueProps {
    firstName: string;
    lastName: string;
    email: string;
    contact: string;
    address1: string;
    address2: string;
}

export interface MockDataTeam {
    id: number;
    name: string;
    email: string;
    age: number;
    phone: string;
    access: string;
}